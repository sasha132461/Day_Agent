export interface IntegrationStatus {
  gmail_connected: boolean;
  telegram_connected: boolean;
  telegram_code_sent: boolean;
  server_telegram_configured: boolean;
  server_gmail_configured: boolean;
}
